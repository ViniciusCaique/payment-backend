
To clone the project
```bash
git clone url
```

To install the dependencies
```bash
npm i
```

To run docker
```bash
docker compose up -d --build
```

To run unit tests
```bash
npm run test:unit
```
